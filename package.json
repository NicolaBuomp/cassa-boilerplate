{
  "name": "cassa-banco",
  "version": "0.1.0",
  "private": true,
  "description": "Boilerplate di cassa per un'attività col banco. Vendite composte da smartphone dallo staff.",
  "//workspaces": "print-server è escluso di proposito: ha una dipendenza nativa da compilare e si installa solo sul PC del banco.",
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspace=@cassa/web",
    "build": "npm run build --workspace=@cassa/web",
    "lint": "npm run lint --workspace=@cassa/web",
    "typecheck": "npm run typecheck --workspace=@cassa/web",
    "test": "npm run test --workspace=@cassa/web",
    "db:reset": "supabase db reset",
    "db:types": "supabase gen types typescript --local --schema public > apps/web/lib/supabase/database.types.ts"
  }
}
